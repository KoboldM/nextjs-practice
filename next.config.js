/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    // basePath: '/nextjs-practice',
    // images: {
    //     unoptimized: true
    // },
}

module.exports = nextConfig
